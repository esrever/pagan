#pragma once

namespace pgn
{
    //! 2D array class for level data of arbitrary datatype. 
    template<class T>
    class cArray2D
    {
        /*
            v = map(row,col)
            map(row,col) = v
            map.rows()
            map.cols()
            T * map.raw()
        */
    };
}