OnEntiyCreated: AFTER components have been added, do glueing stuff like layout to level glue, render priority, etc
EntityType: categories and subcategories


MapRenderOrder (MapSprite)
    DungeonElementBG=0
    DungeonElementFG
    Item
    Creature
    Effect
    UI
    
Create python script to generate the type hierarchy with categories and subcategories?
But there's no single unique type... the above can be used as rendering types (MapRenderOrder)
    
    
// Visitors/Views:
coord place_at_distance ( start )
    start from given coord
    for dir in directions:
        res = CalcPath( start, dir, out_c)
        if res
            break
    
function CalcPath( start, cur, dir)