OnEntiyCreated: AFTER components have been added, do glueing stuff like layout to level glue, render priority, etc
EntityType: categories and subcategories


Tile Types:
    DungeonElementBG
    DungeonElementFG
    Creature
    Item
    Effect
    UI
    
    
/--------------------------------------------------------
    
#include <iostream>
#include <cassert>

using namespace std;

template<class T, size_t N>
struct Enum2String{};

enum eMap
{
    tile,
    creature
};


template<>
struct Enum2String<eMap,size_t(eMap::tile)>{ 
    static const char * Get() {return "tile";}
    
    };
    
template<>
struct StringEnum<eMap>
{     
    static StringEnum& Instance() {static StringEnum se; return se;}
    
    void Add(size_t e, const std::string& s) 
    {
        e2s[e] = s;
    }
    
    const std::string& Get(size_t e) 
    { 
        return e2s[e];
    }
    
    private:
        std::map<size_t, std::string> e2s;
};


// decl bit
template<> StringEnum< E >


int main()
{
   cout << "Hello World" << endl; 
   
   
   cout << Enum2String<eMap, eMap::tile>::Get();
   
   return 0;
}
    