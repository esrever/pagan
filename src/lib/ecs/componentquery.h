#pragma once

namespace pgn
{
	namespace ecs
	{
		//! TODO: listen to entity creation/deletion!
		//	      listen to entity-adds-component from ecs. does that ever happen? not yet, so ignore!
		template<class T>
		class cComponentQuery
		{
			public:
			private:
		};
	}
}